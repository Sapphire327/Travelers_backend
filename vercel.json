{
  "buildCommand": "yarn build && yarn db:deploy",
  "installCommand": "yarn install",
  "framework": "express",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/dist/server.js"
    }
  ],
	"routes": [
		{
			"src": "/(.*)",
			"dest": "/dist/server.js"
		}
	]
}